[project]
name = "molecolyte"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Prajwal Nayaka T", email = "prajwalnayakat@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
molecolyte = "molecolyte:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
